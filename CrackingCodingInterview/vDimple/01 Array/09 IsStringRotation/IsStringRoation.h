#pragma once
#include<iostream>
#include<string>

using namespace std;

__declspec(dllexport) bool isStringRotation(string origString, string rotatedString);
bool isSubstring(string origString, string rotatedString);